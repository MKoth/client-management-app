import { ResizeDetectorDirective } from './resize-detector.directive';

describe('ResizeDetectorDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeDetectorDirective();
    expect(directive).toBeTruthy();
  });
});
